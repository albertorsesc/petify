language: php
php:
  - 7.1
  - 7.2
  - 7.3

before_script:
  - chmod 777 -R storage
  - cp .env.travis .env
  - composer install --no-interaction
  - php artisan key:generate
  - php artisan migrate --seed
  - npm install

cache:
  directories:
    - node_modules
    - vendor

script:
  - vendor/bin/phpunit